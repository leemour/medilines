- if @brands
  = @brands.each(&:inspect)

- if @products
  - @products.each do |product|
    %tr
      %td= product.slug
      %td= product.name
      %td= product.type
      %td= product.price
      %td= product.country
      %td= product.options
      %td= product.features
      %td= product.description
      %td= product.design
      %td= product.slogan
- else
  %p В данной категории товаров нет.